[package]
name = "lit-parser"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = { version = "^1.0", features = ["full"] }
quote = "^1.0"
# bnum = { path = "../" }
proc-macro-error = "1.0.4"
