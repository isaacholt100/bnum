- Add FromBytes, ToBytes, ConstZero, ConstOne from num_traits
- unchecked methods const on stable
- change BTryFrom to TryFrom for BUint to BInt, BInt to BUint
- remove Add<Digit> implementation
- make TryFrom<primitive> instead of From
- only 8 bit digits
- rename to Uint, Int
- add alloc and signed features
- MSRV 1.65.0 -> 1.83.0
- use 2024 edition
- use rand 0.9.0, so update try_fill_slice to fill_slice (no option returned)
- make as_bits_mut, digits_mut, set_bit const
- TODO:
    - carrying_mul_add
    - isqrt (including checked for signed ints)
    - from_ascii
    - from_ascii_radix
    - unchecked_disjoint_bitor (can do by iterating unchecked_disjoint bitor on u8s/u128 digits, can only add this once it is stablised for primitives though, not much point adding on nightly only)
    - complete documentation (not just links to the Rust primitive equivalent)